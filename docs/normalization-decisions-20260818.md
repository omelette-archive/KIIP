# 품목 정규화 판정 출처 공개 — 2026-08-18

## 결론

#51의 자동화 방향 결정("자동화가 기본, 감사 리포트를 남기고 문제 발견 시 로직을 수정")에 따라
②단계 판정에 `verdictSource`를 추가하고 ②→③→④→⑦ 전 구간에 전파했다. 대시보드는 사람이
개별 승인하지 않은 자동 확정 항목에 "AI 판정" 배지를 표시하며, 이 문서는 그 첫 실측 결과다.

## 실측 (기존 raw 원물명 1,721건 재실행, 2026-08-18)

농사로 원본 rawItemName/sido/sigungu(2026-08-14 알파 실행분과 동일 입력)를 최신 코드로
재정규화했다. 자동 확정 826건/검토대기 891건/제외 4건 수치는 기존과 동일하다 — 이번 변경은
확정 여부를 바꾸지 않고, **이미 자동 확정되던 항목의 판정 출처를 처음으로 구분해 노출**한다.

| verdictSource | 건수 | 비고 |
|---|---:|---|
| unresolved | 891 | 사람 검토 필요(변경 없음) |
| algorithm | 527 | 신선한/미가공 접두어 자동 확정 — 이번에 처음 구분 노출 |
| human_approved_alias | 158 | 승인된 별칭 사전 매칭 |
| exact | 141 | 원물명이 고시명칭과 완전 동일 |
| excluded | 4 | 나무/묘목/씨앗 등 규칙 제외 |

## algorithm 판정 표본검사

61개 원물명→고시명칭 조합(527행)을 전수 확인했다. 상위 항목은 배·딸기·사과·토마토·포도 등
흔한 과일·채소류가 "신선한 X" 또는 "미가공 X"로 정확히 매핑된 것으로, 육안 검토상 문제로 보이는
조합은 없었다. 전체 목록은 `node 02-normalize-items/reportNormalizationDecisions.js`로 언제든
재생성할 수 있다(`02-normalize-items/output/normalization-decisions.md`, git-ignored).

## 검토대기(unresolved) 891건은 그대로 사람 검토 대상

`docs/normalization-review-20260814.md`의 결론(상위 15개 군집 중 자동 승인 가능 0개 — 원물과
가공품/생물 상태가 다른 사례가 다수)은 이번 변경으로 바뀌지 않는다. `algorithm` 화이트리스트는
"신선한"/"미가공" 두 접두어만 허용하는 좁은 범위이며, 그 외 애매한 조합(단감→옷단감치는 기계,
매실→매실주 등)은 여전히 문자열 유사도만으로 자동 확정하지 않는다.

## 함께 고친 것 — 후보 점수 경계 버그

`findCandidates`의 문자열 포함 관계 판정이 접두/접미 경계가 아니라 문자열 중간 우연 일치에도
+0.5 보너스를 주고 있었다(예: "단감"이 "옷단감치는 기계" 안에 우연히 끼어 있어도 강한 신호로
취급됨). 접두/접미 경계에서만 보너스를 주도록 수정했다 — 자동 확정 로직 자체는 문자열 점수를
쓰지 않아 영향이 없지만, 검토대기 화면에 표시되는 후보 순위 품질이 개선된다.

## 다음 단계

- 사람이 `normalization-decisions.md`의 algorithm 목록을 주기적으로 훑어보고 이상 항목이 있으면
  `ruleNormalizer.js`의 화이트리스트를 좁힌다.
- 화이트리스트를 넓히려면(예: "생 X", "냉동 X") 실측 근거와 회귀 테스트를 갖춰 명시적으로
  추가한다 — 이 리포트의 신뢰도 점수를 자동 확정 기준으로 직접 쓰지 않는다.
